# 마니또 매칭 서비스 진행상황

**최종 업데이트**: 2025-01-20
**현재 상태**: Phase 2 완료 (핵심 기능 구현 완료)

---

## 📋 구현 완료 기능

### ✅ 호스트 기능
- [x] 호스트 회원가입/로그인 시스템 (bcrypt 해시)
- [x] 모임 생성/조회/삭제
- [x] 호스트 참여 방식 선택
  - 호스트로만 참여 (매칭에 포함, 참가자 목록 별도 표시 안 함)
  - 게스트로도 참여 (참가자로 등록, isHost: true)
- [x] 모임별 6자리 고유 코드 생성
- [x] QR 코드 생성
- [x] 참가자 목록 실시간 조회
- [x] 참가자 삭제 (호스트 참가자는 삭제 불가)
- [x] 마니또 매칭 실행/결과 확인/초기화
- [x] 대시보드 UI (반응형)

### ✅ 참가자 기능
- [x] 2단계 인증 시스템
  - 1단계: 이름 입력 및 중복 체크
  - 2단계: 비밀번호 입력 (신규 가입/재로그인)
- [x] 재로그인 기능 (이름 + 비밀번호)
- [x] 방명록 작성/수정 (로그인 후)
- [x] 참가자 목록 확인
- [x] 마니또 결과 확인
- [x] 참가 화면 UI (반응형)

### ✅ 마니또 매칭 시스템
- [x] Fisher-Yates 셔플 알고리즘
- [x] 원형 연결 방식 (모든 참가자가 1명씩 할당)
- [x] 최소 3명 이상 체크 (상호 매칭 방지)
- [x] 호스트 참여 방식에 따른 자동 조정
- [x] 매칭 결과 저장/조회
- [x] 매칭 초기화 기능

---

## 🗂️ 프로젝트 구조

```
presentparty/
├── public/                    # 프론트엔드 파일
│   ├── index.html            # 메인 랜딩 페이지
│   ├── host/
│   │   ├── index.html        # 호스트 로그인
│   │   └── dashboard.html    # 호스트 대시보드
│   ├── participant/
│   │   ├── join.html         # 참가자 가입/로그인
│   │   └── view.html         # 참가자 화면
│   └── assets/
│       └── css/
│           └── style.css     # 공통 스타일
│
├── src/                      # 백엔드 로직
│   ├── config/
│   │   └── firebase.js       # Firebase Admin SDK 초기화
│   ├── services/             # 비즈니스 로직
│   │   ├── hostService.js    # 호스트 관련
│   │   ├── meetingService.js # 모임 관련
│   │   ├── participantService.js # 참가자 관련
│   │   └── manittoService.js # 매칭 관련
│   └── routes/               # API 라우트
│       ├── hostRoutes.js
│       ├── meetingRoutes.js
│       ├── participantRoutes.js
│       └── manittoRoutes.js
│
├── server.js                 # Express 서버 메인
├── package.json
├── .env                      # 환경변수 (Firebase 설정)
├── serviceAccountKey.json    # Firebase Admin SDK 키
├── 개요.md                   # 프로젝트 개요
└── 진행상황.md               # 본 문서
```

---

## 🔧 주요 기술 스택

### Backend
- **Node.js** + **Express.js** (v4.x)
- **Firebase Admin SDK** (Firestore)
- **bcrypt** (비밀번호 해싱, SALT_ROUNDS: 10)
- **qrcode** (QR 코드 생성)
- **cors** (CORS 처리)

### Frontend
- **Vanilla JavaScript** (ES6+)
- **HTML5** + **CSS3**
- **Fetch API** (REST API 통신)

### Development
- **nodemon** (개발 서버 자동 재시작)
- **dotenv** (환경변수 관리)

---

## 📡 API 엔드포인트

### 호스트 API (`/api/host`)
- `POST /register` - 호스트 회원가입
- `POST /login` - 호스트 로그인
- `GET /:hostId/meetings` - 호스트의 모임 목록 조회

### 모임 API (`/api/meetings`)
- `POST /` - 모임 생성
- `GET /code/:code` - 코드로 모임 조회
- `GET /:meetingId` - ID로 모임 조회
- `PUT /:meetingId` - 모임 수정
- `DELETE /:meetingId` - 모임 삭제
- `GET /:meetingId/participants` - 참가자 목록 조회

### 참가자 API (`/api/participants`)
- `POST /check-name` - 이름 중복 체크
- `POST /` - 참가자 가입
- `POST /login` - 참가자 로그인
- `GET /meeting/:meetingId` - 모임의 참가자 목록
- `PUT /:participantId/message` - 방명록 수정
- `DELETE /:meetingId/:participantId` - 참가자 삭제
- `GET /:meetingId/name/:participantName` - 이름으로 참가자 조회
- `GET /manitto/:meetingId/:participantName` - 마니또 조회

### 마니또 API (`/api/manitto`)
- `POST /execute/:meetingId` - 매칭 실행
- `GET /mappings/:meetingId` - 전체 매칭 결과 조회 (호스트용)
- `DELETE /reset/:meetingId` - 매칭 초기화

---

## 💾 데이터베이스 스키마

### Firestore 컬렉션

#### `hosts`
```javascript
{
  id: "testhost",
  password: "$2b$10$...", // bcrypt 해시
  createdAt: Timestamp
}
```

#### `meetings`
```javascript
{
  title: "2024 송년회 마니또",
  hostId: "testhost",
  code: "ABC123",
  status: "waiting" | "active" | "completed",
  createdAt: Timestamp,
  maxParticipants: 100,
  hostJoinsAsParticipant: false, // 호스트 게스트 참여 여부
  matchedAt: Timestamp // 매칭 완료 시각
}
```

#### `meetings/{meetingId}/participants`
```javascript
{
  name: "홍길동",
  password: "$2b$10$...", // bcrypt 해시
  message: "즐거운 송년회 되세요!",
  isHost: false, // 호스트가 게스트로 참여 시 true
  joinedAt: Timestamp
}
```

#### `meetings/{meetingId}/manittoMappings`
```javascript
{
  giver: "홍길동",
  receiver: "김철수",
  createdAt: Timestamp
}
```

---

## ⚙️ 환경 설정

### `.env` 파일
```env
NODE_ENV=development
PORT=3000
BASE_URL=http://localhost:3000
```

### `serviceAccountKey.json`
Firebase Admin SDK 서비스 계정 키 파일 (Firebase Console에서 다운로드)

---

## 🎯 핵심 로직 설명

### 1. 호스트 참여 방식

#### 호스트로만 참여 (`hostJoinsAsParticipant: false`)
- 매칭에는 포함되지만 참가자 컬렉션에 등록되지 않음
- 매칭 시 `allParticipants = [hostId, ...participants]`
- 참가자 수: `participants.length` (호스트 제외)
- 대시보드에서 호스트 정보를 상태 밑에 별도 표시

#### 게스트로도 참여 (`hostJoinsAsParticipant: true`)
- 참가자 컬렉션에 `isHost: true`로 등록
- 매칭 시 `allParticipants = participants` (이미 포함됨)
- 참가자 수: `participants.length` (호스트 포함)
- 참가자 목록에서 👑 아이콘 + "호스트" 배지로 표시
- 삭제 불가 (`isHost: true` 체크)

### 2. 참가자 인증 플로우

```
1. 참가자가 모임 코드로 접속
   ↓
2. 이름 입력 → 중복 체크 API 호출
   ↓
3-a. 중복된 이름 → 로그인 폼 표시 (비밀번호 입력)
3-b. 새로운 이름 → 가입 폼 표시 (비밀번호 + 확인)
   ↓
4. 인증 성공 → 참가자 화면 (방명록 수정 가능)
```

### 3. 마니또 매칭 알고리즘

```javascript
// Fisher-Yates 셔플
function shuffleArray(array) {
  const shuffled = [...array];
  for (let i = shuffled.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
  }
  return shuffled;
}

// 원형 연결
const shuffled = shuffleArray(allParticipants);
for (let i = 0; i < shuffled.length; i++) {
  const giver = shuffled[i];
  const receiver = shuffled[(i + 1) % shuffled.length]; // 원형 연결
  // 매핑 저장...
}
```

**특징:**
- 최소 3명 필요 (2명일 경우 A↔B 상호 매칭 발생)
- 모든 참가자가 정확히 1명의 마니또를 담당
- 마지막 참가자는 첫 번째 참가자를 마니또로 담당

### 4. 비밀번호 보안

```javascript
// 가입 시
const hashedPassword = await bcrypt.hash(password, SALT_ROUNDS);

// 로그인 시
const isValid = await bcrypt.compare(password, hashedPassword);

// API 응답 시 비밀번호 제외
const { password, ...participantData } = data;
```

---

## 🚨 주요 이슈 및 해결사항

### Issue #1: 참가자 목록이 표시되지 않음
**원인**: `getMeetingParticipants()` 함수에서 비밀번호 필드를 포함하여 반환
**해결**: 비밀번호 필드를 destructuring으로 제외
```javascript
const { password, ...participantData } = data;
```

### Issue #2: 호스트 게스트 참여 정보가 DB에 저장되지 않음
**원인**: `meetingRoutes.js`에서 `hostJoinsAsParticipant` 관련 필드를 `req.body`에서 추출하지 않음
**해결**: 필요한 필드들을 추출하여 `createMeeting()`에 전달

### Issue #3: 참가자 수 카운팅 오류
**원인**: 호스트 참여 방식을 고려하지 않은 카운팅
**해결**: 항상 `participants.length`만 표시 (호스트는 카운팅에서 제외)

---

## 📝 코드 컨벤션

### 비동기 처리
- `async/await` 사용
- 모든 비동기 함수에 `try-catch` 에러 핸들링

### 네이밍
- 함수: camelCase (예: `createMeeting`)
- 상수: UPPER_SNAKE_CASE (예: `SALT_ROUNDS`)
- 라우트: kebab-case (예: `/check-name`)

### 주석
- 각 함수 위에 JSDoc 스타일 주석
```javascript
/**
 * 모임 생성
 */
async function createMeeting(hostId, meetingData) {
  // ...
}
```

---

## 🔄 개발 워크플로우

### 서버 실행
```bash
npm run dev  # nodemon으로 자동 재시작
```

### 코드 수정 시
1. 파일 수정
2. nodemon이 자동으로 서버 재시작
3. 브라우저에서 확인

### 포트 충돌 해결 (Windows)
```bash
# 포트 사용 프로세스 확인
netstat -ano | findstr :3000

# 프로세스 종료
taskkill //PID {PID} //F
```

---

## ⏭️ 다음 단계 (미구현 기능)

### Phase 3: UI/UX 개선
- [ ] 로딩 상태 표시
- [ ] 에러 메시지 개선
- [ ] 모바일 최적화
- [ ] 애니메이션 추가

### Phase 4: 추가 기능
- [ ] 참가자 화면 실시간 업데이트
- [ ] 프로필 이미지 업로드
- [ ] 매칭 결과 공유 기능
- [ ] 통계 대시보드

### Phase 5: 수익화
- [ ] Google AdSense 통합
- [ ] 프리미엄 기능 (대용량 모임 등)
- [ ] SEO 최적화

---

## 💡 개발 시 주의사항

### 1. Firebase 보안
- `serviceAccountKey.json`은 절대 Git에 커밋하지 않음
- `.env` 파일도 `.gitignore`에 추가

### 2. 비밀번호 처리
- API 응답에서 항상 비밀번호 필드 제외
- bcrypt 해싱 사용 (SALT_ROUNDS: 10)

### 3. 호스트 참가자 처리
- `isHost: true`인 참가자는 삭제 불가
- 매칭 시 `hostJoinsAsParticipant` 확인 필수

### 4. 최소 인원 체크
- 매칭 실행 전 최소 3명 확인
- 호스트 참여 방식에 따라 카운팅 다름

### 5. 에러 핸들링
- 모든 API 엔드포인트에 try-catch
- 적절한 HTTP 상태 코드 반환
- 사용자 친화적인 에러 메시지

---

## 🐛 알려진 이슈

현재 없음

---

## 📞 참고 자료

- [개요.md](./개요.md) - 프로젝트 전체 개요
- [README.md](./README.md) - API 문서 및 사용법
- [Firebase 공식 문서](https://firebase.google.com/docs)
- [Express.js 공식 문서](https://expressjs.com/)

---

*이 문서는 개발 진행에 따라 지속적으로 업데이트됩니다.*
<br>

---

## ✨ 최근 개선사항 (2025년 11월 20일)

### 🚀 성능 개선
- **API 속도 제한 최적화**: `server.js`에서 `speedLimiter` 미들웨어의 적용 범위를 전역에서 `/api/` 경로로 제한하여, 페이지 로딩 및 정적 파일 요청 시 발생하던 불필요한 지연을 제거했습니다. 이로 인해 메인 페이지에서 "호스트로 시작하기" 버튼 클릭 시의 화면 전환 속도 등 전반적인 사용자 경험이 크게 개선되었습니다.
- **로그인 UX 개선**: `host-login.js`에서 로그인 성공 후 불필요하게 1초간 대기하던 `setTimeout`을 제거하여, 성공 즉시 대시보드로 이동하도록 수정했습니다. 이를 통해 사용자가 느끼는 로그인 속도를 크게 향상시켰습니다.
- **N+1 쿼리 문제 해결**: `hostService.js`의 `getHostMeetings` 함수에서 각 모임의 참가자 수를 순차적으로 조회하던 로직을 `Promise.all`을 사용하여 병렬로 처리하도록 개선했습니다. 이로써 호스트 대시보드에서 모임 목록을 불러오는 속도가 크게 향상되었습니다.

### 🔒 보안 강화 (CSP 위반 수정)
- 모든 사용자 인터페이스 HTML 파일 (`public/index.html`, `public/host/login.html`, `public/host/dashboard.html`, `public/participant/join.html`)에서 콘텐츠 보안 정책(CSP)을 위반하는 인라인 JavaScript 코드 (예: `onclick`, `onchange` 속성 및 `<script>` 블록)를 제거했습니다.
- 제거된 JavaScript 로직은 각 페이지에 해당하는 외부 `.js` 파일(`public/assets/js/main-page.js`, `public/assets/js/host-login.js`, `public/assets/js/host-dashboard.js`, `public/assets/js/participant-join.js`)으로 분리되었습니다.
- 이벤트 핸들링은 `DOMContentLoaded` 이벤트 리스너와 이벤트 위임(Event Delegation) 방식을 사용하여 외부 스크립트에서 안전하게 처리되도록 변경했습니다. 이는 XSS 공격 방어에 기여하고 코드 유지보수성을 향상시킵니다.

### 🛠️ 코드 품질 및 무결성 향상 (2025년 11월 20일)
- **트랜잭션 무결성 보장:** `manittoService.js`의 `executeMatching`, `resetMatching` 함수와 `meetingService.js`의 `createMeeting`, `deleteMeeting` 함수에서 여러 Firestore 쓰기 작업이 필요한 로직을 `db.batch()`를 사용한 원자적 일괄 쓰기로 리팩토링했습니다. 이는 작업 중간에 오류 발생 시 데이터 불일치를 방지하여 데이터 무결성을 강화합니다.
- **호스트 권한 확인 미들웨어 도입:** `manittoRoutes.js`의 중복된 호스트 권한 확인 로직을 `src/middleware/auth.js` 파일 내 `isHost` 미들웨어로 추출하여 적용했습니다. 이를 통해 코드 중복을 제거하고 라우트 핸들러의 가독성 및 유지보수성을 향상시켰습니다.

---