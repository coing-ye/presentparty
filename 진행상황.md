# 마니또 매칭 서비스 진행상황

**최종 업데이트**: 2025-11-25
**현재 상태**: Phase 3 완료 (날짜 투표 시스템 구현 완료)

---

## 📋 구현 완료 기능

### ✅ 호스트 기능
- [x] 호스트 회원가입/로그인 시스템 (bcrypt 해시)
- [x] 모임 생성/조회/삭제
- [x] 호스트 참여 방식 선택
  - 호스트로만 참여 (매칭에 포함, 참가자 목록 별도 표시 안 함)
  - 게스트로도 참여 (참가자로 등록, isHost: true)
- [x] 모임별 6자리 고유 코드 생성
- [x] QR 코드 생성
- [x] 참가자 목록 실시간 조회
- [x] 참가자 삭제 (호스트 참가자는 삭제 불가)
- [x] 마니또 매칭 실행/결과 확인/초기화
- [x] **날짜 투표 시스템**
  - 모임 가능 날짜 범위 설정
  - 호스트 날짜 선호도 투표 (선호/일반/비선호/불가능)
  - 참가자별 투표 현황 조회
  - 날짜별 참가 가능 인원 및 선호도 순위 확인
  - 투표 이력 유지 (재오픈 시 이전 선택 표시)
- [x] 대시보드 UI (반응형)

### ✅ 참가자 기능
- [x] 2단계 인증 시스템
  - 1단계: 이름 입력 및 중복 체크
  - 2단계: 비밀번호 입력 (신규 가입/재로그인)
- [x] 재로그인 기능 (이름 + 비밀번호)
- [x] 방명록 작성/수정 (로그인 후)
- [x] 참가자 목록 확인
- [x] 마니또 결과 확인
- [x] **날짜 투표 참여**
  - 날짜별 선호도 투표 (선호/일반/비선호/불가능)
  - 투표 이력 유지 및 수정 가능
  - 평일/주말 일괄 설정 토글
- [x] 참가 화면 UI (반응형)

### ✅ 마니또 매칭 시스템
- [x] Fisher-Yates 셔플 알고리즘
- [x] 원형 연결 방식 (모든 참가자가 1명씩 할당)
- [x] 최소 3명 이상 체크 (상호 매칭 방지)
- [x] 호스트 참여 방식에 따른 자동 조정
- [x] 매칭 결과 저장/조회
- [x] 매칭 초기화 기능

### ✅ 보안 및 검증
- [x] Rate Limiting (IP별 요청 제한)
  - 전역 제한: 15분당 100회
  - 인증 제한: 15분당 5회
  - 생성 작업: 15분당 10회
  - 매칭 실행: 5분당 3회
- [x] Input Validation (express-validator)
  - XSS 방지 (escape 처리)
  - SQL Injection 방지
  - 입력값 길이 및 형식 검증
- [x] Security Headers (Helmet)
  - CSP (Content Security Policy)
  - XSS Protection
  - MIME Type Sniffing 방지
- [x] 호스트 권한 확인 미들웨어
- [x] Speed Limiter (과도한 요청 시 응답 지연)

### ✅ 성능 및 UX 개선
- [x] 로딩 인디케이터 추가
- [x] API 요청 최적화 (N+1 쿼리 제거)
- [x] 병렬 처리 (Promise.all)
- [x] 트랜잭션 무결성 보장 (Firestore batch)
- [x] 에러 메시지 개선

---

## 🗂️ 프로젝트 구조

```
presentparty/
├── public/                    # 프론트엔드 파일
│   ├── index.html            # 메인 랜딩 페이지
│   ├── host/
│   │   ├── login.html        # 호스트 로그인
│   │   └── dashboard.html    # 호스트 대시보드
│   ├── participant/
│   │   └── join.html         # 참가자 가입/로그인
│   └── assets/
│       ├── css/
│       │   └── style.css     # 공통 스타일
│       └── js/               # 클라이언트 JavaScript (CSP 준수)
│           ├── firebase-client.js   # Firebase 클라이언트 초기화
│           ├── loading.js           # 로딩 인디케이터
│           ├── main-page.js         # 메인 페이지 로직
│           ├── host-login.js        # 호스트 로그인 로직
│           ├── host-dashboard.js    # 호스트 대시보드 로직
│           └── participant-join.js  # 참가자 참가 로직
│
├── src/                      # 백엔드 로직
│   ├── config/
│   │   └── firebase.js       # Firebase Admin SDK 초기화
│   ├── middleware/           # 미들웨어
│   │   ├── auth.js           # 호스트 권한 확인
│   │   ├── security.js       # Rate Limiting, Helmet
│   │   └── validation.js     # Input Validation
│   ├── services/             # 비즈니스 로직
│   │   ├── hostService.js    # 호스트 관련
│   │   ├── meetingService.js # 모임 관련
│   │   ├── participantService.js # 참가자 관련
│   │   ├── manittoService.js # 매칭 관련
│   │   └── dateVotingService.js # 날짜 투표 관련
│   └── routes/               # API 라우트
│       ├── hostRoutes.js
│       ├── meetingRoutes.js
│       ├── participantRoutes.js
│       ├── manittoRoutes.js
│       └── dateVotingRoutes.js
│
├── server.js                 # Express 서버 메인
├── package.json
├── .env                      # 환경변수 (Firebase 설정)
├── serviceAccountKey.json    # Firebase Admin SDK 키
├── 개요.md                   # 프로젝트 개요
└── 진행상황.md               # 본 문서
```

---

## 🔧 주요 기술 스택

### Backend
- **Node.js** + **Express.js** (v4.x)
- **Firebase Admin SDK** (Firestore)
- **bcrypt** (비밀번호 해싱, SALT_ROUNDS: 10)
- **qrcode** (QR 코드 생성)
- **cors** (CORS 처리)
- **helmet** (보안 헤더 설정)
- **express-rate-limit** (Rate Limiting)
- **express-slow-down** (Speed Limiter)
- **express-validator** (입력값 검증)

### Frontend
- **Vanilla JavaScript** (ES6+)
- **HTML5** + **CSS3**
- **Fetch API** (REST API 통신)

### Development
- **nodemon** (개발 서버 자동 재시작)
- **dotenv** (환경변수 관리)

---

## 📡 API 엔드포인트

### 호스트 API (`/api/host`)
- `POST /register` - 호스트 회원가입
- `POST /login` - 호스트 로그인
- `GET /:hostId/meetings` - 호스트의 모임 목록 조회

### 모임 API (`/api/meetings`)
- `POST /` - 모임 생성
- `GET /code/:code` - 코드로 모임 조회
- `GET /:meetingId` - ID로 모임 조회
- `PUT /:meetingId` - 모임 수정
- `DELETE /:meetingId` - 모임 삭제
- `GET /:meetingId/participants` - 참가자 목록 조회

### 참가자 API (`/api/participants`)
- `POST /check-name` - 이름 중복 체크
- `POST /` - 참가자 가입
- `POST /login` - 참가자 로그인
- `GET /meeting/:meetingId` - 모임의 참가자 목록
- `PUT /:participantId/message` - 방명록 수정
- `DELETE /:meetingId/:participantId` - 참가자 삭제
- `GET /:meetingId/name/:participantName` - 이름으로 참가자 조회
- `GET /manitto/:meetingId/:participantName` - 마니또 조회

### 마니또 API (`/api/manitto`)
- `POST /execute/:meetingId` - 매칭 실행
- `GET /mappings/:meetingId` - 전체 매칭 결과 조회 (호스트용)
- `DELETE /reset/:meetingId` - 매칭 초기화

### 날짜 투표 API (`/api/date-voting`)
- `POST /:meetingId/setup` - 날짜 투표 설정 (호스트 전용)
- `GET /:meetingId` - 날짜 투표 정보 조회
- `POST /:meetingId/vote` - 참가자 투표 제출
- `GET /:meetingId/vote/:participantName` - 특정 참가자의 투표 조회
- `GET /:meetingId/results` - 투표 결과 및 날짜별 순위 조회

---

## 💾 데이터베이스 스키마

### Firestore 컬렉션

#### `hosts`
```javascript
{
  id: "testhost",
  password: "$2b$10$...", // bcrypt 해시
  createdAt: Timestamp
}
```

#### `meetings`
```javascript
{
  title: "2024 송년회 마니또",
  hostId: "testhost",
  code: "ABC123",
  status: "waiting" | "active" | "completed",
  createdAt: Timestamp,
  maxParticipants: 100,
  hostJoinsAsParticipant: false, // 호스트 게스트 참여 여부
  matchedAt: Timestamp, // 매칭 완료 시각
  dateVoting: { // 날짜 투표 설정 (선택)
    enabled: true,
    startDate: Timestamp, // 투표 가능 시작일
    endDate: Timestamp,   // 투표 가능 종료일
    hostVote: {
      impossible: ["2025-11-28", "2025-11-29"],
      notPreferred: ["2025-11-30"],
      preferred: ["2025-12-01"]
    },
    createdAt: Timestamp
  }
}
```

#### `meetings/{meetingId}/participants`
```javascript
{
  name: "홍길동",
  password: "$2b$10$...", // bcrypt 해시
  message: "즐거운 송년회 되세요!",
  isHost: false, // 호스트가 게스트로 참여 시 true
  joinedAt: Timestamp
}
```

#### `meetings/{meetingId}/manittoMappings`
```javascript
{
  giver: "홍길동",
  receiver: "김철수",
  createdAt: Timestamp
}
```

#### `meetings/{meetingId}/participantDateVotes`
```javascript
{
  participantName: "홍길동",
  impossible: ["2025-11-28", "2025-11-29"], // 참가 불가능한 날짜
  notPreferred: ["2025-11-30"],             // 비선호 날짜
  preferred: ["2025-12-01", "2025-12-02"],  // 선호 날짜
  updatedAt: Timestamp
}
```

---

## ⚙️ 환경 설정

### `.env` 파일
```env
NODE_ENV=development
PORT=3000
BASE_URL=http://localhost:3000
```

### `serviceAccountKey.json`
Firebase Admin SDK 서비스 계정 키 파일 (Firebase Console에서 다운로드)

---

## 🎯 핵심 로직 설명

### 1. 호스트 참여 방식

#### 호스트로만 참여 (`hostJoinsAsParticipant: false`)
- 매칭에는 포함되지만 참가자 컬렉션에 등록되지 않음
- 매칭 시 `allParticipants = [hostId, ...participants]`
- 참가자 수: `participants.length` (호스트 제외)
- 대시보드에서 호스트 정보를 상태 밑에 별도 표시

#### 게스트로도 참여 (`hostJoinsAsParticipant: true`)
- 참가자 컬렉션에 `isHost: true`로 등록
- 매칭 시 `allParticipants = participants` (이미 포함됨)
- 참가자 수: `participants.length` (호스트 포함)
- 참가자 목록에서 👑 아이콘 + "호스트" 배지로 표시
- 삭제 불가 (`isHost: true` 체크)

### 2. 참가자 인증 플로우

```
1. 참가자가 모임 코드로 접속
   ↓
2. 이름 입력 → 중복 체크 API 호출
   ↓
3-a. 중복된 이름 → 로그인 폼 표시 (비밀번호 입력)
3-b. 새로운 이름 → 가입 폼 표시 (비밀번호 + 확인)
   ↓
4. 인증 성공 → 참가자 화면 (방명록 수정 가능)
```

### 3. 마니또 매칭 알고리즘

```javascript
// Fisher-Yates 셔플
function shuffleArray(array) {
  const shuffled = [...array];
  for (let i = shuffled.length - 1; i > 0; i--) {
    const j = Math.floor(Math.random() * (i + 1));
    [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
  }
  return shuffled;
}

// 원형 연결
const shuffled = shuffleArray(allParticipants);
for (let i = 0; i < shuffled.length; i++) {
  const giver = shuffled[i];
  const receiver = shuffled[(i + 1) % shuffled.length]; // 원형 연결
  // 매핑 저장...
}
```

**특징:**
- 최소 3명 필요 (2명일 경우 A↔B 상호 매칭 발생)
- 모든 참가자가 정확히 1명의 마니또를 담당
- 마지막 참가자는 첫 번째 참가자를 마니또로 담당

### 4. 비밀번호 보안

```javascript
// 가입 시
const hashedPassword = await bcrypt.hash(password, SALT_ROUNDS);

// 로그인 시
const isValid = await bcrypt.compare(password, hashedPassword);

// API 응답 시 비밀번호 제외
const { password, ...participantData } = data;
```

### 5. 날짜 투표 시스템

#### 날짜별 점수 계산 알고리즘

```javascript
function calculateDateScore(date, allVotes) {
  let availableCount = 0;  // 참가 가능한 인원 수
  let totalScore = 0;       // 총 선호도 점수
  const unavailableParticipants = [];  // 불가능한 참가자 목록

  const dateStr = date.toISOString().split('T')[0];

  allVotes.forEach((vote) => {
    // 불가능한 날인지 확인
    if (vote.impossible.includes(dateStr)) {
      unavailableParticipants.push(vote.participantName);
      return;
    }

    // 참가 가능
    availableCount++;

    // 점수 계산
    if (vote.preferred.includes(dateStr)) {
      totalScore += 3;  // 선호
    } else if (vote.notPreferred.includes(dateStr)) {
      totalScore += 1;  // 비선호
    } else {
      totalScore += 2;  // 일반 (선택 안함)
    }
  });

  return {
    date: dateStr,
    availableCount,
    totalScore,
    unavailableParticipants
  };
}
```

**점수 체계:**
- **선호 날짜**: 3점
- **일반 날짜** (선택 안함): 2점
- **비선호 날짜**: 1점
- **불가능 날짜**: 점수 없음 (참가 불가능 인원에 포함)

**순위 정렬 기준:**
1. 참가 가능 인원 수 (내림차순) - 우선순위 1
2. 총 선호도 점수 (내림차순) - 우선순위 2

**특징:**
- 호스트와 참가자의 투표가 동일한 가중치로 계산됨
- 투표하지 않은 날짜는 "일반" 날짜로 간주 (2점)
- 범위 밖 날짜는 UI에 표시되지만 순위 계산에는 포함되지 않음
- 투표 이력은 유지되어 재설정 시 이전 선택이 표시됨

#### 날짜 포맷팅 (타임존 이슈 해결)

```javascript
// ❌ 잘못된 방법 (타임존 변환으로 날짜 이동)
const dateStr = new Date(2025, 10, 25).toISOString().split('T')[0];
// → "2025-11-24" (KST → UTC 변환으로 하루 빠름)

// ✅ 올바른 방법 (로컬 타임존 유지)
function formatDateLocal(date) {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  return `${year}-${month}-${day}`;
}
const dateStr = formatDateLocal(new Date(2025, 10, 25));
// → "2025-11-25" (정확함)
```

---

## 🚨 주요 이슈 및 해결사항

### Issue #1: 참가자 목록이 표시되지 않음
**원인**: `getMeetingParticipants()` 함수에서 비밀번호 필드를 포함하여 반환
**해결**: 비밀번호 필드를 destructuring으로 제외
```javascript
const { password, ...participantData } = data;
```

### Issue #2: 호스트 게스트 참여 정보가 DB에 저장되지 않음
**원인**: `meetingRoutes.js`에서 `hostJoinsAsParticipant` 관련 필드를 `req.body`에서 추출하지 않음
**해결**: 필요한 필드들을 추출하여 `createMeeting()`에 전달

### Issue #3: 참가자 수 카운팅 오류
**원인**: 호스트 참여 방식을 고려하지 않은 카운팅
**해결**: 항상 `participants.length`만 표시 (호스트는 카운팅에서 제외)

### Issue #4: 날짜 투표 시작일이 선택 범위에 포함되지 않음
**원인**: `toISOString()` 메서드가 로컬 시간을 UTC로 변환하면서 타임존 차이로 인해 날짜가 하루 빠르게 변환됨
- 예: 한국 시간 2025-11-25 00:00:00 → UTC 2025-11-24T15:00:00.000Z
**해결**: `formatDateLocal()` 헬퍼 함수를 생성하여 타임존 변환 없이 로컬 날짜를 포맷팅
```javascript
function formatDateLocal(date) {
  const year = date.getFullYear();
  const month = String(date.getMonth() + 1).padStart(2, '0');
  const day = String(date.getDate()).padStart(2, '0');
  return `${year}-${month}-${day}`;
}
```

### Issue #5: 투표 결과 조회 시 500 Internal Server Error
**원인**: `dateVotingService.js`의 `calculateDateScore()` 함수에서 `dateStr` 변수가 `forEach` 루프 내부에 선언되었으나 함수의 return 문에서 참조됨 (변수 스코프 오류)
**해결**: `dateStr` 변수를 `forEach` 루프 밖으로 이동하여 함수 전체에서 접근 가능하도록 수정

### Issue #6: 날짜 투표 모달의 버튼 높이 불일치
**원인**: `.btn-secondary` 클래스에 전역으로 설정된 `margin-top: 10px`이 flex 레이아웃 내에서 "설정완료"와 "취소" 버튼의 높이 차이를 만듦
**해결**: 두 버튼에 인라인 스타일 `margin-top: 0;`을 추가하여 전역 스타일 오버라이드

### Issue #7: 투표 데이터 영속성 부재
**원인**: 날짜 투표 모달을 재오픈 시 기존 투표 데이터를 불러오지 않아 매번 초기 상태로 표시됨
**해결**:
- 호스트: `showDateVotingSetup()` 함수를 async로 변경하고 API를 통해 기존 투표 데이터 조회 및 UI 초기화
- 참가자: 기존 `loadMyDateVote()` 함수 활용
- 달력 UI에 이전 선택 상태를 색상으로 표시
- 범위 밖 날짜는 50% 투명도로 표시하여 시각적으로 구분

---

## 📝 코드 컨벤션

### 비동기 처리
- `async/await` 사용
- 모든 비동기 함수에 `try-catch` 에러 핸들링

### 네이밍
- 함수: camelCase (예: `createMeeting`)
- 상수: UPPER_SNAKE_CASE (예: `SALT_ROUNDS`)
- 라우트: kebab-case (예: `/check-name`)

### 주석
- 각 함수 위에 JSDoc 스타일 주석
```javascript
/**
 * 모임 생성
 */
async function createMeeting(hostId, meetingData) {
  // ...
}
```

---

## 🔄 개발 워크플로우

### 서버 실행
```bash
npm run dev  # nodemon으로 자동 재시작
```

### 코드 수정 시
1. 파일 수정
2. nodemon이 자동으로 서버 재시작
3. 브라우저에서 확인

### 포트 충돌 해결 (Windows)
```bash
# 포트 사용 프로세스 확인
netstat -ano | findstr :3000

# 프로세스 종료
taskkill //PID {PID} //F
```

---

## ⏭️ 다음 단계 (미구현 기능)

### Phase 3: UI/UX 개선
- [x] 로딩 상태 표시
- [x] 에러 메시지 개선
- [ ] 모바일 최적화
- [ ] 애니메이션 추가
- [ ] 다크 모드 지원

### Phase 4: 추가 기능
- [ ] 참가자 화면 실시간 업데이트 (WebSocket/Firestore 리스너)
- [ ] 프로필 이미지 업로드
- [ ] 매칭 결과 공유 기능 (SNS 공유)
- [ ] 통계 대시보드 (참가자 활동 분석)
- [ ] 이메일 알림 기능
- [ ] 모임 종료 후 기록 보관

### Phase 5: 배포 및 운영
- [ ] CI/CD 파이프라인 구축
- [ ] 모니터링 시스템 (로그, 에러 추적)
- [ ] 백업 및 복구 시스템
- [ ] 성능 모니터링 (APM)

### Phase 6: 수익화
- [ ] Google AdSense 통합
- [ ] 프리미엄 기능 (대용량 모임, 우선 지원 등)
- [ ] SEO 최적화
- [ ] 랜딩 페이지 개선

---

## 💡 개발 시 주의사항

### 1. Firebase 보안
- `serviceAccountKey.json`은 절대 Git에 커밋하지 않음
- `.env` 파일도 `.gitignore`에 추가

### 2. 비밀번호 처리
- API 응답에서 항상 비밀번호 필드 제외
- bcrypt 해싱 사용 (SALT_ROUNDS: 10)

### 3. 호스트 참가자 처리
- `isHost: true`인 참가자는 삭제 불가
- 매칭 시 `hostJoinsAsParticipant` 확인 필수

### 4. 최소 인원 체크
- 매칭 실행 전 최소 3명 확인
- 호스트 참여 방식에 따라 카운팅 다름

### 5. 에러 핸들링
- 모든 API 엔드포인트에 try-catch
- 적절한 HTTP 상태 코드 반환
- 사용자 친화적인 에러 메시지

### 6. 입력값 검증
- express-validator를 사용한 모든 입력값 검증
- XSS 공격 방지 (escape 처리)
- 입력값 길이 및 형식 제한

### 7. Rate Limiting
- 엔드포인트별 적절한 Rate Limit 설정
- 인증 관련 엔드포인트는 더 엄격한 제한 적용
- Speed Limiter로 과도한 요청 시 응답 지연

### 8. 미들웨어 사용
- `isHost` 미들웨어로 호스트 권한 확인
- 라우트별 적절한 validation 미들웨어 적용
- security 미들웨어는 server.js에서 전역 또는 경로별로 설정

### 9. 트랜잭션 처리
- 여러 문서를 동시에 수정할 때는 Firestore batch 사용
- 원자성 보장으로 데이터 불일치 방지

---

## 🐛 알려진 이슈

현재 없음

---

## 📊 프로젝트 통계

- **총 라인 수**: ~3,500+ 라인
- **API 엔드포인트**: 22개 (날짜 투표 5개 포함)
- **미들웨어**: 3개 (auth, security, validation)
- **서비스 모듈**: 5개 (dateVotingService 추가)
- **라우트 모듈**: 5개 (dateVotingRoutes 추가)
- **클라이언트 JS 파일**: 6개
- **Firestore 컬렉션**: 2개 (hosts, meetings)
- **Firestore 서브컬렉션**: 3개 (participants, manittoMappings, participantDateVotes)

---

## 📞 참고 자료

- [개요.md](./개요.md) - 프로젝트 전체 개요
- [README.md](./README.md) - API 문서 및 사용법
- [Firebase 공식 문서](https://firebase.google.com/docs)
- [Express.js 공식 문서](https://expressjs.com/)

---

*이 문서는 개발 진행에 따라 지속적으로 업데이트됩니다.*
<br>

---

## ✨ 최근 개선사항 (2025년 11월)

### 🚀 성능 개선
- **API 속도 제한 최적화**: `server.js`에서 `speedLimiter` 미들웨어의 적용 범위를 전역에서 `/api/` 경로로 제한하여, 페이지 로딩 및 정적 파일 요청 시 발생하던 불필요한 지연을 제거했습니다. 이로 인해 메인 페이지에서 "호스트로 시작하기" 버튼 클릭 시의 화면 전환 속도 등 전반적인 사용자 경험이 크게 개선되었습니다.
- **로그인 UX 개선**: `host-login.js`에서 로그인 성공 후 불필요하게 1초간 대기하던 `setTimeout`을 제거하여, 성공 즉시 대시보드로 이동하도록 수정했습니다. 이를 통해 사용자가 느끼는 로그인 속도를 크게 향상시켰습니다.
- **N+1 쿼리 문제 해결**: `hostService.js`의 `getHostMeetings` 함수에서 각 모임의 참가자 수를 순차적으로 조회하던 로직을 `Promise.all`을 사용하여 병렬로 처리하도록 개선했습니다. 이로써 호스트 대시보드에서 모임 목록을 불러오는 속도가 크게 향상되었습니다.
- **마니또 매칭 지연 개선**: 매칭 실행 시 응답 속도를 최적화하여 사용자 대기 시간을 단축했습니다.

### 🔒 보안 강화
- **CSP 위반 수정**: 모든 사용자 인터페이스 HTML 파일 (`public/index.html`, `public/host/login.html`, `public/host/dashboard.html`, `public/participant/join.html`)에서 콘텐츠 보안 정책(CSP)을 위반하는 인라인 JavaScript 코드 (예: `onclick`, `onchange` 속성 및 `<script>` 블록)를 제거했습니다.
- **외부 스크립트 분리**: 제거된 JavaScript 로직은 각 페이지에 해당하는 외부 `.js` 파일(`public/assets/js/main-page.js`, `public/assets/js/host-login.js`, `public/assets/js/host-dashboard.js`, `public/assets/js/participant-join.js`)으로 분리되었습니다.
- **이벤트 핸들링 개선**: `DOMContentLoaded` 이벤트 리스너와 이벤트 위임(Event Delegation) 방식을 사용하여 외부 스크립트에서 안전하게 처리되도록 변경했습니다. 이는 XSS 공격 방어에 기여하고 코드 유지보수성을 향상시킵니다.
- **보안 미들웨어 추가**: Rate Limiting, Input Validation, Security Headers 등 다층 보안 체계 구축

### 🛠️ 코드 품질 및 무결성 향상
- **트랜잭션 무결성 보장**: `manittoService.js`의 `executeMatching`, `resetMatching` 함수와 `meetingService.js`의 `createMeeting`, `deleteMeeting` 함수에서 여러 Firestore 쓰기 작업이 필요한 로직을 `db.batch()`를 사용한 원자적 일괄 쓰기로 리팩토링했습니다. 이는 작업 중간에 오류 발생 시 데이터 불일치를 방지하여 데이터 무결성을 강화합니다.
- **호스트 권한 확인 미들웨어 도입**: `manittoRoutes.js`의 중복된 호스트 권한 확인 로직을 `src/middleware/auth.js` 파일 내 `isHost` 미들웨어로 추출하여 적용했습니다. 이를 통해 코드 중복을 제거하고 라우트 핸들러의 가독성 및 유지보수성을 향상시켰습니다.
- **마니또 매칭 로직 수정**: 매칭 알고리즘의 안정성과 정확성을 개선했습니다.

### 🎨 UI/UX 개선
- **초대링크 인라인 개선**: 참가 링크 및 QR 코드 표시 방식을 개선하여 사용자 경험을 향상시켰습니다.
- **로딩 인디케이터 추가**: API 요청 시 로딩 상태를 시각적으로 표시하여 사용자 피드백을 개선했습니다.

### 📅 날짜 투표 시스템 개선 (2025년 11월 25일)

#### 타임존 문제 해결
- **문제**: `toISOString()` 사용 시 로컬 시간이 UTC로 변환되면서 날짜가 하루 빠르게 표시되는 현상 발생
  - 예: 2025-11-25 00:00:00 KST → 2025-11-24T15:00:00.000Z → "2025-11-24"
- **해결**: `formatDateLocal()` 헬퍼 함수를 생성하여 타임존 변환 없이 로컬 날짜를 YYYY-MM-DD 형식으로 포맷팅
- **적용 파일**: `host-dashboard.js`, `participant-join.js`의 모든 날짜 포맷팅 로직

#### 투표 데이터 영속성 구현
- **문제**: 투표 모달을 재오픈 시 이전 선택이 초기화되어 사용자가 처음부터 다시 선택해야 함
- **해결**:
  - 호스트: `showDateVotingSetup()` 함수를 async로 변경하고 기존 투표 데이터를 API에서 조회하여 로드
  - 참가자: 기존 `loadMyDateVote()` 함수를 활용하여 모달 오픈 시 자동으로 이전 투표 데이터 로드
  - 달력 UI에 이전 선택 상태를 색상으로 표시 (선호: 초록, 비선호: 노랑, 불가능: 빨강)
- **추가 기능**: 투표 범위가 변경되어도 이전 선택은 유지되어 달력에 표시되지만, 범위 밖 날짜는 50% 투명도로 표시하고 클릭 비활성화

#### 변수 스코핑 버그 수정
- **문제**: `dateVotingService.js:218`에서 `ReferenceError: dateStr is not defined` 오류 발생
- **원인**: `calculateDateScore()` 함수에서 `dateStr` 변수가 `forEach` 루프 내부에 선언되었으나 return 문에서 참조됨
- **해결**: `dateStr` 변수 선언을 `forEach` 루프 밖으로 이동하여 함수 전체 스코프에서 접근 가능하도록 수정

#### 버튼 레이아웃 수정
- **문제**: 날짜 투표 모달의 "설정완료"와 "취소" 버튼의 높이가 달라 보이는 시각적 불일치
- **원인**: `.btn-secondary` 클래스에 전역으로 설정된 `margin-top: 10px`이 flex 레이아웃에서 버튼 높이 차이를 만듦
- **해결**: 인라인 스타일 `margin-top: 0;`을 추가하여 전역 스타일을 오버라이드

#### 날짜 범위 시각화 개선
- **기능**: 투표 가능한 날짜 범위를 벗어난 날짜는 50% 투명도로 표시하고 클릭 비활성화
- **사용자 경험**: 이전에 선택한 날짜가 범위 밖이 되더라도 회색으로 표시되어 어떤 날짜를 선택했었는지 확인 가능
- **데이터 무결성**: 백엔드에서는 현재 투표 범위 내의 날짜만 순위 계산에 포함

---